remind_me <- function() {
  errands <- c("pay rent", "file taxes", "get milk", "get flour")
  print(errands)
  call <- c("Ash", "Mona", "Mom", "Dad", "Gargi", "Stephen")
  print(call)
  
}
remind_me()